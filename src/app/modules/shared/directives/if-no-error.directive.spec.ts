import {IfNoErrorDirective} from './no-error.directive';

describe('IfNoErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new IfNoErrorDirective();
    expect(directive).toBeTruthy();
  });
});
