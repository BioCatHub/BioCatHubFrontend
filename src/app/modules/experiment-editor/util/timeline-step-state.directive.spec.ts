import {TimelineStepStateDirective} from './timeline-step-state.directive';

describe('TimelineStepStateDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineStepStateDirective();
    expect(directive).toBeTruthy();
  });
});
