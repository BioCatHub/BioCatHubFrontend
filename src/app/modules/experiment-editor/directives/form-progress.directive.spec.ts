import {FormProgressDirective} from './form-progress.directive';

describe('FormProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new FormProgressDirective();
    expect(directive).toBeTruthy();
  });
});
